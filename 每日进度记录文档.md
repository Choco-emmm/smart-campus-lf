# 每日进度记录文档

## 第一天

### 1. 项目进度

- 选定了题目2，分析业务+设计表结构中


### 2. 明日目标

- 开始构建项目基本架构
***

## 第二天

### 1. 项目进度

- 分析了题目，明确了自己要实现的功能
- 根据需求建了需要的7张表
- 根据表把项目架构好了
- 将上一个项目的utils，interceptor等迁移了过来
- 实现了注册和登录功能
  - 注册使用BCrypt编码，登录使用BCrypt，不在数据库中存储明文密码
  - 登录会添加token+在redis设置过期时间
  - 使用`query.er(User::getUsername)`，MyBatis-Plus 利用了 Lambda 的可序列化特性和反射机制在运行时动态推导出数据库列名，从而避免sql注入
- 实现了通用唯一性校验 ，供输入失焦后校验
  - 使用枚举类，在controller传入type时做枚举类绑定，从而避免sql注入


### 2. 明日目标

- 了解BCrypt给密码加密的原理
- 了解Lambda 的可序列化特性和反射机制动态推导出数据库列名的原理
- 实现查看个人信息、修改个人信息（支持自定义昵称和头像，头像这个存在本地先吧，用上上一个项目的数据库只存图片名的思想）、修改密码的接口
***
## 第三天

### 1. 项目进度

- 增加了查询失物信息详情的功能
- 新增上传失物图片功能
- 新增上传失物信息功能
- 将用户的活跃信息存入redis，使用定时任务每10分钟将所有用户的活跃信息存进数据库
- 增加上传/修改头像功能
- 增加所填旧密码是否与原密码相同校验的功能，供输入失焦后校验
- 增加更新密码功能
- 增加更新用户基本信息（手机号，邮箱，昵称）功能
- 增加获取用户个人信息功能

### 2. 明日目标

- 了解hutool的copyproperties的原理
- 实现失物信息的修改和删除
- 实现失物信息分页+多条件查询
- 实现举报功能
- 去看spring AI相关

  ***
## 第四天

### 1. 项目进度

  - 实现了失物/拾取信息的**修改**与**删除**功能，并在 Service 层通过 `UserContext` 严格校验操作人权限，防止非法越权 。
  - 新增了**物品状态更新接口**，并引入业务联动逻辑：当物品状态变更为“已结案”时，系统会自动取消其置顶状态并清空过期时间 。
  - 利用 MyBatis-Plus 分页插件实现了分页查询，支持按物品类型过滤、关键字（标题或名称）模糊搜索 
  - 实现了基于 `is_top`（置顶）倒序和 `create_time`（发布时间）倒序的复合排序，确保置顶帖始终在最上方 
  - 在分页转换 VO 过程中，实现了提取详情表第一张图作为封面展示的逻辑 
- 优化了详情查询，当发布者账号已注销时，系统能优雅显示“用户已注销”而非直接报错或空指针
- 实现了用户对违规信息的投诉链路，引入`ReportRecordEnum` 规范化管理“待处理、已核实、已驳回”状态
- 上线了 `ItemTopExpireTask` 扫描器，每分钟自动检测并撤下超时的置顶帖子，保证平台公平性 
### 2. 明日目标
- 开始管理员模块

- 了解@TableLogic的作用
***
## 第五天

### 1. 项目进度

- 明确并落地了**“双轨制”状态管理架构**，严格区分了用户的“逻辑删除（`@TableLogic`）”与管理员的“违规下架（`status=3`）”。确保违规帖子从公共广场消失，但仍保留在用户的“我的发布”列表中以作案底警示。
- 实现了**举报审批与跨表联动机制**，管理员核实举报属实后，系统通过 `@Transactional` 事务自动将原帖状态修改为违规下架，并强制扒除其可能存在的“置顶”特权。
- 实现了**置顶申请的完整闭环**，支持动态条件分页查询置顶申请单；审批通过后自动修改物品主表状态并计算置顶过期时间（默认24小时），完美衔接前期的定时自动降级任务。
- 实现了**后台用户治理模块**：
  - 运用 MyBatis-Plus 的 `LambdaQueryWrapper` 优雅解决了状态筛选（`AND`）与多字段模糊搜索（嵌套 `OR`）的 SQL 拼接冲突问题。
  - 新增用户档案详情接口，通过 Mapper 动态执行 `COUNT` 统计用户的历史违规发帖次数，避免了数据冗余与一致性问题。
- **修复了安全漏洞**，在查询层和操作层双管齐下，严格拦截了试图查看或封禁同级管理员账号的越权请求，保障了系统底线安全。
- 实现了 **JWT 动态踢人与封禁机制**，创新性地复用 Redis 已有 Key，将封禁用户的 Token 值替换为 `banned` 并沿用原 TTL，配合 `TokenInterceptor` 拦截器改造。
- 落地了**前后台场景分离**设计，新增 “他人公开主页”接口，返回极致脱敏的 `UserProfileVO`，并巧妙保留 `status` 字段供前端渲染“已被封禁”提示，优化了用户间的社交沟通体验。

### 2. 明日目标
- 开始留言模块

- 开始看怎样使用多模态AI
***

## 第五天

### 1. 项目进度

- **完善了互动留言模块**
  - 实现了查看某个帖子下所有留言的功能。
  - 实现了自动消除小红点功能：发帖人点进帖子看留言时，未读留言会自动变成已读状态。
- **开发了私信单聊系统**
  - 实现了“消息会话列表”，用代码将乱糟糟的私信按聊天对象自动分组，并把有最新消息的会话顶到最上方。
- **增加了消息通知提醒**
  - 实现了留言通知功能，能按帖子把“未读留言”归类，提示用户“哪个帖子有几条新留言”。
  - 给私信页面加上了极简版的未读消息小红点统计。
- **成功接入本地 AI 大模型**
  - 使用 Spring AI 成功连上了本地的 qwen3.5 大模型。
  - 实现了普通用户发帖的“AI 代写”功能：AI 能根据物品名和地点，自动生成一段高情商的寻物/招领启事并存入数据库。

### 2. 明日目标

- 实现管理端定期调用ai总结物品数据
- 进展一些进阶功能

***

## 第六天

### 1. 项目进度

- **主攻前端页面，把前后端接口都对接起来了**
- **重构了地址选择体验（发布与修改）**
  - 把填地址的地方换成了“级联选择 + 手动自填”的组合组件（比如先选“教学楼-一号楼”，再自己填“205室”），前端会在提交时自动把两部分拼成字符串发给后端。
  - 写了个拆分函数，在修改帖子回显数据时，能把后端传过来的长地址智能拆分回级联框和输入框里。
- **修复并完善了广场的搜索筛选**
  - 解决了之前按状态、地点搜索参数发不出去的 bug。统一了查询逻辑，现在只要切换条件或点击搜索，前端会强制拼装好完整的地址再发请求，并且会自动重置回第一页。
- **搭建了管理员后台的三个专属页面**
  - **数据看板**：把基础的统计数据展示出来了。引入了 `marked` 插件，把后端返回的 AI 周报（Markdown 格式）渲染成了带标题、加粗和列表的漂亮排版，去掉了多余的手动刷新。
  - **用户管理**：做了一个带分页的表格，能直接一键封禁/解封用户。加了个详细档案弹窗（能看到违规次数等后台数据），并且把列表里的头像和昵称都做成了可以点击跳到他个人主页的链接。
  - **信息审核（举报与置顶）**：用双 Tab 页把“举报单处理”和“置顶申请”分开了。给举报单做了个专门的详情弹窗来对付长篇大论的举报理由，还能直接在里面点“审阅原帖”弹出一个快捷看帖子的窗口，不用跳来跳去。
- **优化了帖子详情页的展示与权限**
  - 修好了发帖人头像取错字段显示不出来的 bug。
  - 同样用 `marked` 插件把 AI 自动润色的物品描述加上了渐变背景展示在页面里。
  - 做了权限区分：管理员看帖子时不显示“举报”，直接显示“一键违规下架”和“一键置顶”。
- **优化了私信聊天和个人主页的体验**
  - 把私信左侧列表的时间去掉了，让昵称显示更宽敞清爽。
  - 做了一个很方便的联动：在帖子或者主页点击“私聊联系”跳到消息页时，会自动读取地址栏参数并选中跟这个人的对话；如果以前没聊过，就自动新建一个会话顶在最前面。
  - 给管理员开了个后门，逛别人主页的时候也能直接看到并点击“封禁该账号”按钮。
- **配置了前端路由**：把后台的几个页面加到了路由表里，并且在导航栏写了判断，只有管理员登录时才显示后台入口。

### 2. 明日目标

- 做进阶功能
***